import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import Button from '.';

<Meta
  title="components/Home/Button"
  argTypes={{ handleClick: { action: 'clicked' } }}
  component={Button}
/>

# Button

Botão usado para Login

<ArgsTable of={Button} />

<Canvas>
  <Story
    name="Button Component"
    args={{
      ...Button.defaultProps,
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
