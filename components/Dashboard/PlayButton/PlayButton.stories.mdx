import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import PlayButton from '.';
import styled from 'styled-components';

<Meta
  title="components/Dashboard/PlayButton"
  argTypes={{
    handlePlay: { action: 'clicked' },
  }}
  component={PlayButton}
/>

# PlayButton

Botão utilizado para Play/Pause.

<Canvas>
  <Story
    name="PlayButton Component"
    args={{
      ...PlayButton.defaultProps,
      show: true,
    }}
  >
    {(args) => {
      const Container = styled.div`
        position: relative;
        width: 50px;
        height: 50px;
      `;
      return (
        <Container>
          <PlayButton {...args} />
        </Container>
      );
    }}
  </Story>
</Canvas>

<ArgsTable of={PlayButton} />
