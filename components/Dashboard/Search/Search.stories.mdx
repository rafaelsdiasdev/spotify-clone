import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import Search from '.';

<Meta
  title="components/Dashboard/Search"
  argTypes={{ handlePlay: { action: 'clicked' } }}
  component={Search}
/>

# Search

Search de artista ou musica

<ArgsTable of={Search} />

<Canvas>
  <Story
    name="Search Component"
    args={{
      ...Search.defaultProps,
      pageSearch: true,
    }}
  >
    {(args) => <Search {...args} />}
  </Story>
</Canvas>
